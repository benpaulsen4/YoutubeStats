<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ConfigEditor.GroupsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ConfigEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" Margin="10,10,10,5" HorizontalAlignment="Center">
            <Button Content="Group" Margin="0,0,5,0"/>
            <Button Content="Sub-Group" Margin="0,0,5,0"/>
            <Button Content="Creator"/>
        </StackPanel>
        <ListView ItemsSource="{x:Bind Items}" Margin="10,5,10,5" Height="326" Width="329"/>
        <StackPanel Orientation="Horizontal" Margin="10,5,10,10" HorizontalAlignment="Center">
            <Button Content="Load Config" Margin="0,0,5,0"/>
            <Button Content="Edit General" Click="ShowGeneral" Margin="0,0,5,0"/>
            <Button Content="Save Changes" />
        </StackPanel>
    </StackPanel>
</Page>
